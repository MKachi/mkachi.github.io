{
  "version": "0.2.0",
  "configurations": [{
    "name": "Firefox",
    "type": "firefox",
    "request": "launch",
    "url": "http://localhost:9000",
    "webRoot": "${workspaceFolder}"
  }, {
    "name": "Chrome",
    "type": "chrome",
    "request": "launch",
    "url": "http://localhost:9000",
    "webRoot": "${workspaceFolder}"
  }]
}
